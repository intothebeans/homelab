# Generated compose file for imported stack: caddy
# This was automatically generated by Arcane from an external stack.
# The original compose file could not be read from: /home/admin/containers/caddy/docker-compose.yml.
# You may need to adjust this manually for correct operation.

services:
  caddy:
    build: ./Dockerfile
    container_name: caddy
    user: 400:33
    restart: always
    network_mode: host
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
      - /mnt/ssd/remote-data/dav/:/dav:rw
    labels:
      - diun.enable=false
    env_file: .env
volumes:
  caddy_data:
  caddy_config:
